(function () {
    'use strict';

    var loginFactory = function ($resource, $q) {

        //Login Factory API
        return {

        }

    };

    angular.module('app')
        .factory('loginFactory', ['$resource', '$q', loginFactory]);

})();